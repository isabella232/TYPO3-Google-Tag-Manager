language: php

php:
  - 5.5
  - 5.6
  - 7.0

env:
  - TYPO3_VERSION=^7.6 COVERAGE=0 FUNCTIONAL=0
  - TYPO3_VERSION=dev-master COVERAGE=0 FUNCTIONAL=0

matrix:
  exclude:
    - env: TYPO3_VERSION=dev-master COVERAGE=0 FUNCTIONAL=0
      php: 5.5
    - env: TYPO3_VERSION=dev-master COVERAGE=0 FUNCTIONAL=0
      php: 5.6
  include:
    - php: 5.5
      env: TYPO3_VERSION=^7.6 COVERAGE=1 FUNCTIONAL=0
  allow_failures:
    - env: TYPO3_VERSION=dev-master COVERAGE=0 FUNCTIONAL=1
    - env: TYPO3_VERSION=dev-master COVERAGE=0 FUNCTIONAL=0

sudo: false

addons:
  apt:
    packages:
      - parallel

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - composer self-update
  - composer --version

before_script:
  # Installs TYPO3
  - composer require --dev typo3/cms=$TYPO3_VERSION;
  - export "TYPO3_PATH_WEB"=$PWD/.Build/Web;
  # Restore composer.json
  - git checkout composer.json;

script:
  # Execute unit tests
  - >
    echo "Running unit tests";
    if [[ "$COVERAGE" == "1" ]]; then
      .Build/bin/phpunit --coverage-clover=unittest-coverage.clover --colors -c Tests/phpunit.xml

      echo "Uploading code coverage results";
      wget https://scrutinizer-ci.com/ocular.phar
      php ocular.phar code-coverage:upload --format=php-clover unittest-coverage.clover
    else
      .Build/bin/phpunit --colors -c Tests/phpunit.xml
    fi

  # Execute functional tests, if enabled
  - >
    if [[ "$FUNCTIONAL" == "1" ]]; then
      echo "Running functional tests"
      export typo3DatabaseHost="localhost";
      export typo3DatabaseName="typo3";
      export typo3DatabaseUsername="root";
      export typo3DatabasePassword="";
      .Build/bin/phpunit -c .Build/Web/typo3/sysext/core/Build/FunctionalTests.xml Tests/Functional/
    fi

  - >
    echo;
    echo "Running php lint";
    find . -name \*.php ! -path "./vendor/*" ! -path "./.Build/*" ! -path "./scripts/*" ! -path "./typo3_src/*" ! -path "./typo3temp/*" | parallel --gnu php -d display_errors=stderr -l {} > /dev/null \;
